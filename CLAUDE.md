# 株式会社 業務の改善 — サイト制作ガイド

## プロジェクト概要

- サイト: gyokai.jp（株式会社 業務の改善）
- ホスティング: GitHub Pages（`main` ブランチのルートから配信）
- CSS: Tailwind CSS CDN + カスタムクラス
- フォント: Noto Sans JP / Noto Serif JP（Google Fonts）
- カラー: ocean（#2952b8 系）/ ink（#2d3a50 系）

## ディレクトリ構成

```
/
├── index.html              # トップページ
├── article-template.html   # GAS で記事生成するときのテンプレート
├── article.css             # 記事共通スタイル
├── articles/               # 記事ページ（サブディレクトリ）
│   └── *.html              # パスは ../ で上位を参照
├── assets/                 # 画像・SVG
└── gas/
    └── Code.gs             # Google Docs → article HTML 変換スクリプト
```

## GAS パイプライン

Google Docs → `gas/Code.gs`（Parser方式）→ Drive に HTML 保存 → 手動で `articles/` に配置 → push

テンプレートプレースホルダー:

```
{{PAGE_TITLE}} {{TITLE}} {{CATEGORY}} {{DATE}} {{LEAD}}
<!-- ARTICLE_CONTENT -->
```

---

## 記事ページ制作レギュレーション

### 思想

記事は「CMS量産ページ」ではなく **「読み物」** として設計する。

情報を届けるのではなく、**思想と空気感を届ける** ためのもの。

### 空気感

- 静か、誠実
- 売り込み感を出さない
- 余白を大切にする
- 温度感を持たせる
- "作品感" を少し残す
- note のような読みやすさ

### フォント

ページ種別によってルールが異なる。

| ページ種別 | 見出し（h1/h2/h3） | 本文・リスト |
|---|---|---|
| トップ・サービス・代表等（通常ページ） | **Noto Serif JP**（明朝） | Noto Sans JP（ゴシック） |
| 記事詳細ページ（`articles/*.html`） | **Noto Sans JP**（ゴシック） | Noto Sans JP（ゴシック） |

**通常ページ**: `h1, h2, h3, .serif { font-family: 'Noto Serif JP', serif; }` をグローバルに設定。本文はゴシック。

**記事ページのみ**: 見出しを含む全文ゴシック統一。serif は使わない。長文耐性・スマホ可読性のため。

### 本文カラー

```css
/* 推奨 */
color: #222;       /* または */
color: #1f2937;
```

薄すぎるグレー（#6b7280 以下）を本文に使うことは禁止。

### レイアウト

| 項目 | 指定 |
|---|---|
| 本文幅 | `max-w-3xl`〜`max-w-4xl`（読み疲れ防止） |
| 余白 | 広め。段落間は十分に取る |
| reveal アニメーション | 静かに、速度遅め（0.65s〜0.75s） |

### 画像ルール

- 横幅いっぱい（full-bleed）禁止
- コンテナ内に収めて角丸 + 薄シャドウ
- キャプションは薄めの小文字（0.72rem、muted カラー）
- "記事に馴染ませる" — 画像が浮き上がらないように

### リスト（`*` 箇条書き）

- `・` や `*` で始まる箇条書きは **太字（font-weight: 700）** で表示する
- マーカーは ocean-300（`#85afff`）の丸点（`::before` で実装）

### 禁止事項

- LP感・過剰装飾
- 強すぎる CTA（色・サイズの主張）
- 派手なアニメーション
- serif の使用
- marketing 臭のある文言・デザイン

### responsive

スマホ可読性最優先。フォントサイズ・余白をモバイルで必ず確認する。

### 記事制作フロー

1. **ChatGPT** — 原稿・構成・壁打ち
2. **Claude Code** — HTML レイアウト・演出
3. **GitHub push** — 公開

---

## 404.html の保守

### おすすめリンクの同期

`404.html` の「もしかして、探していたのはこちらですか？」セクションは、
サービスページの増減・改名があった場合に**手動で更新が必要**。

対象リンク（現在）:

| 表示テキスト | リンク先 |
|---|---|
| ITなんでも相談 | `it-consulting.html` |
| 伴走型改善支援 | `support.html` |
| 本格開発支援 | `development.html` |
| 代表について | `about.html` |
| ニュース＆ストーリーズ | `index.html#news` |

**サービスページを追加・削除・改名したときは、必ず `404.html` のおすすめリンクも確認・更新すること。**
